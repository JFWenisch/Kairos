ALTER TABLE resource_type_config
    ADD COLUMN IF NOT EXISTS embed_policy VARCHAR(32) NOT NULL DEFAULT 'DISABLED';

CREATE TABLE IF NOT EXISTS embed_allowed_origin (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    origin VARCHAR(255) NOT NULL UNIQUE
);
